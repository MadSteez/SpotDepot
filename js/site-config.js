// Fill these in once, right after creating your GitHub repo, so every
// device that opens this page — phones, in-app browsers, custom domains,
// shortened links, whatever — reliably reads the same shared spot list.
// This takes priority over guessing the repo from the page's own URL,
// which is what mobile browsers and in-app links can trip up.
//
// Leave a value as "" and the app will fall back to auto-detecting it
// from the address bar instead (only works for a plain
// https://<owner>.github.io/<repo>/ GitHub Pages URL, not custom domains).

export const SITE_CONFIG = {
  owner: "MadSteez",
  repo: "SpotDepot",
  branch: "", // e.g. "main" — leave blank to use the repo's default branch
};
